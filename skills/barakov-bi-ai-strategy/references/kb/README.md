# KB — база знаний скилла

Атомарная база знаний: **один файл = одна тема**, связи через `[[wiki-ссылки]]`, машиночитаемый граф — `30-graph.yaml`.

Источники, разобранные на атомы:
- **BI Strategy & Tactics Guide 2.0** (Google Sheets, DataNature) — канонический метод, 22 листа
- **Miro-борд `o9J_lha8MnM=`** — фреймы «BI Strategy Guide 2.0» (зеркало таблицы) и «BI Project Innovation Map» (каталог направлений)
- **Курс «BI+AI стратегия 26»** — девять занятий, PDF-версии как читались участникам
- **Пре-курс опрос участников** — 12 российских BI-проектов, июль–август 2026

## Структура

Нумерация общая с companion-репозиторием [DG Strategy](https://github.com/alexbarakov/DG-strategy-self-consulting-agent): один и тот же номер означает одну и ту же роль в обоих репозиториях.

| Папка / файл | Что внутри | Файлов |
|---|---|---|
| `10-ai-era/` | современный слой: концепты, кейсы и практики девяти занятий курса | 44 |
| `11-method/` | канонический метод: спайн гайда и каждый его лист отдельным атомом | 19 |
| `20-catalog/` | сырой каталог направлений (BI Project Innovation Map) | 1 |
| `30-graph.yaml` | машиночитаемый граф: блок стратегии → атомы, ключевые числа, кейсы | — |
| `31-field/` | полевые данные: бенчмарк участников, карта болей | 2 |
| `50-failure-catalog.md` | 75 названных провалов в 7 семействах с триажем по симптому | — |
| `51-numbers.md` | реестр чисел с тегами надёжности и датами сверки | — |
| `60-roadmap.md` | чего базе не хватает и в каком порядке чинить — по находкам, а не по пожеланиям | — |

## Соглашения по frontmatter

```yaml
id:          # kebab-case, совпадает с именем файла
title:       # заголовок
type:        # method | pattern | evidence | case | benchmark | catalog | metric
source:      # первоисточник
confidence:  # проверяемо | мерил продавец | нет данных | оценка автора | смешанная
origin:      # опционально: пометка для кейсов из практики с обезличенными числами
blocks:      # к каким блокам стратегии относится (1, 2, 3, 4.1, 4.2, 4.3, 5, 6, 7)
```

## Правила использования агентом

1. **Точка входа — `30-graph.yaml`**: по номеру блока стратегии находишь список релевантных атомов.
2. **Цитируй атом, а не пересказывай KB целиком** — файлы нарезаны по одной теме именно для точечного цитирования.
3. **Переноси метку `confidence` вместе с цифрой.** Цифра с пометкой «мерил продавец» не превращается в факт при переносе в артефакт.
4. **Атомы с полем `origin` — разобранные кейсы из практики с обезличенными числовыми порогами.** Переносится конструкция и принцип; пороги ставятся от своего бейзлайна, а не копируются. Формулировки вида «примерно втрое», «около двух третей» — намеренные: важен порядок эффекта, а не чужая цифра.
5. **KB — источник, а не шаблон вывода.** Артефакт собирается по `strategy-template.md`, KB его заземляет.
